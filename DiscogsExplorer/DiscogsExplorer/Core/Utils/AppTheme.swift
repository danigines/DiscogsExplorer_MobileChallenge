//  AppTheme.swift
//  DiscogsExplorer
//  Created by Daniel Garcia on 17/07/25.

import SwiftUI

// Centralized color palette with support for light/dark themes.
struct AppTheme {
  // MARK: - Backgrounds
  static let card = Color.appCard
  static let background = Color.appBackground
  static let placeholderBackground = Color.appPlaceholderBackground

  // MARK: - Text
  static let primaryText = Color.appTextPrimary
  static let secondaryText = Color.appTextSecondary
  static let placeholderText = Color.appPlaceholderText

  // MARK: - Accent / Brand
  static let accent = Color.appAccent
}
